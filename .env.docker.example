# Copy to .env at the repo root — docker compose reads it automatically
# for ${VAR} substitution in docker-compose.yml.

POSTGRES_DB=aap
POSTGRES_USER=aap
POSTGRES_PASSWORD=aap

DJANGO_SECRET_KEY=change-me
FIELD_ENCRYPTION_KEY=change-me-32-bytes-url-safe-base64
GOOGLE_OAUTH_CLIENT_ID=
